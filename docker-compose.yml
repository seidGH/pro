  # Frontend Service
services:
  frontend:
    image: seiddk/pro-frontend:latest
    restart: always
    ports:
      - "3000:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
