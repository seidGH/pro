  # Frontend Service
services:
  frontend:
    image: seiddk/pro-frontend:latest
    restart: always
    ports:
      - "80:7777"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
