  # Frontend Service
services:
  frontend:
    image: seiddk/pro-frontend:latest
    restart: always
    ports:
      - "3333:7777"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
