generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Part {
  id     Int     @id @default(autoincrement())
  name   String
  model  String
  price  Float
  stock  Int
}
